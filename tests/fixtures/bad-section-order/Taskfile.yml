version: '3'

vars:
  APP: myapp

includes:
  dev:
    taskfile: .taskfiles/dev.yml

tasks:
  default:
    desc: List all available tasks
    silent: true
    cmd: task --list

  test:
    desc: Run tests (shortcut)
    cmds:
      - task: dev:test
