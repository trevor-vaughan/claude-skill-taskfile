version: '3'

includes:
  dev:
    taskfile: .taskfiles/dev.yml

vars:
  APP: '{{.APP | default "myapp"}}'

tasks:
  default:
    silent: true
    cmd: task --list

  test:
    desc: "Alias for dev:test"
    cmds:
      - task: dev:test

  check:
    desc: Run all quality gates
    cmds:
      - task: dev:test
