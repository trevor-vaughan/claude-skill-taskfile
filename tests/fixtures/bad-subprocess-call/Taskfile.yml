version: '3'

includes:
  dev:
    taskfile: .taskfiles/dev.yml

tasks:
  default:
    desc: List all available tasks
    silent: true
    cmd: task --list

  test:
    desc: "Alias for dev:test"
    cmd: task dev:test
